# Optional JARs

Optional third-party JARs that are not redistributed with Solace go here (`libs/*.jar` is gitignored).

Quest Helper is not required to build — install it through RuneLite's plugin hub if you use `SolaceQuestHelperPlugin`.
